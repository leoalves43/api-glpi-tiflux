# Decision log

2026-09-21 | New `GlpiClient.obter_anexos_do_followup()` (`GET /ITILFollowup/{id}/Document_Item`); `TifluxClient.publicar_resposta_cliente()`/`publicar_resposta_agente()` gained an optional `anexos` param, sent as repeated `files[]` multipart fields alongside the answer (both endpoints support up to 10 files/request per the OpenAPI spec); `sincronizacao_followups._publicar_followup_no_tiflux()` now fetches and attaches each followup's own docs, routing anything past the 10-file cap to the existing ticket-level `enviar_anexos()` as a fallback (logged, not silently dropped) | User reported GLPI #34234 (Tiflux #362601): a spreadsheet a requester attached to a GLPI followup showed up in Tiflux only as a ticket-level file, not tied to the answer. Live diagnosis (read-only GLPI/Tiflux/Postgres queries): the followup's document (GLPI doc 17453, `tickets_id=0`) never appears in `/Ticket/{id}/Document_Item` — that endpoint only returns docs linked directly to the ticket (doc 17451, the one from ticket creation, `Anexos: 1 enviado(s)` in the audit row). `_sincronizar_anexos()` also only runs once, at ticket creation. So `sincronizar_followups_glpi_para_tiflux()` had NO code path for followup attachments at all — they were silently dropped by the integration; the log/DB trail (`api_glpi_tiflux_followups` id 14111) confirms the answer was published with only text. The second file that did end up on the Tiflux ticket was not created by any code path here — most likely added by hand | supersedes nothing; purely additive, `obter_anexos()`/`_sincronizar_anexos()` (ticket-creation-time attachments) untouched

2026-09-17 | `definir_autor_glpi()` now always returns `config.id_glpi_leo`, independent of mesa — dropped its `id_mesa` param and every caller's mesa-lookup (`_atribuir_tecnico_glpi`, `_encerrar_em_cascata`, `sincronizar_followups_tiflux_para_glpi`, all now simpler too: `_encerrar_em_cascata` no longer needs `ticket_tiflux`, `sincronizar_followups_tiflux_para_glpi` dropped that param entirely) | User: Sania's GLPI user was deactivated; her id (4816, `config.id_glpi_sania`) is renamed "Suporte Embras" going forward — every ticket's GLPI technician/followup-author must be id 4988 (`id_glpi_leo`) regardless of mesa. `config.id_glpi_sania` kept (not deleted) because `_followups_glpi_pendentes()`'s anti-echo filter (`ids_proprios`) still needs it to recognize followups the integration authored as Sania in the past, before this change | supersedes the 2026-09-09 "mesa ARRECADAÇÃO -> Léo, else -> Sania" rule for `definir_autor_glpi()`; `definir_tecnico()` (Tiflux-side technician, unrelated function, ARRECADAÇÃO-only) untouched

2026-09-17 | `GlpiClient.solucao_registrada()` now ignores refused (`status==4`) solutions — only counts as "já registrada" if at least one non-refused `ITILSolution` exists | User noticed: closing Tiflux #362498 a SECOND time after the first refusal did move GLPI #34187's status to Solucionado, but never registered a new solution content — the ticket kept showing the stale, already-refused first solution forever. Root cause: `solucao_registrada()` (`_encerrar_em_cascata`'s idempotency guard, added 2026-09-09 for the single-close case) checked "does any ITILSolution exist" with no status filter — true forever after the FIRST close, so every subsequent re-close-after-refusal skipped `registrar_solucao()` entirely. Confirmed live: registered a second solution on #34187 by hand while a refused one already existed — GLPI accepted it (POST 200) and even auto-flipped ticket status to 5 as a side effect, proving this isn't a GLPI-side restriction, just a missing status check on our side | supersedes nothing; extends the 2026-09-09 entry's idempotency guard to survive multiple close/refuse/reopen cycles, which didn't exist as a concept when that guard was written (reopen-after-refusal only shipped 2026-09-16)

2026-09-17 | `db_followups.registrar_resultado_followup()`'s `ON CONFLICT (direcao, id_origem) DO UPDATE SET` now includes `tipo = EXCLUDED.tipo` (was missing) | Corrects the mechanism described in the entry below this one: the "masking" bug was NOT `_encerrar_em_cascata` racing to overwrite a fresh `'erro'` row — `tipo` was simply never in the UPDATE SET clause, so the single cascade-status row per chamado (`id_origem=-id_glpi`) had its `tipo` frozen at whatever the FIRST insert ever wrote, forever, regardless of which action (encerramento/reabertura/reabertura_tiflux) actually succeeded most recently. `obter_ultima_acao_cascata_sucesso()` reads that frozen `tipo`, so the cascade decision logic was working off stale state indefinitely. Caught live on GLPI #34187: after the disapproval_reason fix (entry above) let the ticket reopen correctly, the user closed Tiflux #362498 a SECOND time — instead of cascading the close to GLPI, the integration reopened Tiflux again, because the row's `tipo` was still `'encerramento'` from the very first write at 14:28:08 even though the real latest successful action (14:56:53) was a `reabertura_tiflux`. Production audit table scan found 2 more chamados with the same frozen-`tipo` symptom (#33898, #33753) — both already closed on both sides (dormant, no current impact; will self-correct on their next real cascade write). #34187's row manually repaired via one-off `UPDATE ... SET tipo='reabertura_tiflux' WHERE id_origem=-34187` (user-approved) to unblock it immediately | supersedes the "masking bug" mechanism described in the 2026-09-17 (disapproval_reason) and 2026-09-16 (403) entries below — same symptom, different and more fundamental root cause than either assumed

2026-09-17 | `TifluxClient.reabrir_ticket()` now requires a `motivo_reprovacao` arg and sends `{"disapproval_reason": ...}` in the PUT body (was `json={}`); `_reabrir_tiflux_apos_recusa_glpi()` passes a generic "Solução recusada pelo requerente no GLPI (chamado #{id_glpi})" reason | User created GLPI #34187 specifically to test the 2026-09-16 cascade-reopen fix, refused the solution, and it didn't reopen Tiflux #362498. Live diagnosis: no 403 this time (permission question from the previous BLOCKED entry is still open — #362498 was in Tiflux's "pending review" window, the free/no-permission-needed path per the OpenAPI spec's badges, so this doesn't confirm the permission was granted; #362124 from the prior entry, fully closed, remains unverified). Calling `reabrir_ticket("362498")` live returned 422 `error_code 42207`: `"This ticket is in review. To reopen by disapproval, it is necessary to send the reason in the 'disapproval_reason' atribute"` — the OpenAPI spec confirms reopening a ticket pending review is "reopen by disapproval" and requires that field, which the code never sent. This 422 is what the known audit-masking bug (see entry below) buried: `_reabrir_tiflux_apos_recusa_glpi` wrote `'reabertura_tiflux'/'erro'` at 14:30, then `_encerrar_em_cascata` re-fired in the same/next pass (Tiflux still `is_closed`) and overwrote the single cascade row back to `'encerramento'/'sucesso'` at 14:33:25 (tentativas 1→3, `atualizado_em` matches GLPI's `solvedate` exactly) — GLPI #34187 got pushed back to Solucionado, which is why the user saw the refusal "not stick" | supersedes nothing; narrows the 2026-09-16 403 entry's scope (permission grant still unconfirmed for the fully-closed case) and identifies the concrete bug the masking entry's `'erro'` row was hiding. Masking bug itself (item 2 in HANDOFF NEXT) is still not fixed — out of scope for this change

2026-09-16 | Diagnosed "GLPI #34018 reaberto mas não reabriu no Tiflux": `TifluxClient.reabrir_ticket()` (shipped hours earlier, same day, see entry below) gets a live `403 {"error_code":40301,"message":"Access Denied - You cannot access this route","detail":{"error":["You are not allowed to review this ticket"]}}` from the real Tiflux API — confirmed by calling it directly against Tiflux #362124 (mesa ARRECADAÇÃO, desk 37964). Not a code bug: the integration's API token has no Tiflux-side permission to reopen ("review") tickets. Root cause traced from the audit table alone before the live call: the single cascade-status row for #34018 (`direcao='tiflux_para_glpi', id_origem=-34018`) had `tentativas=3` but `tipo` still `'encerramento'`/`'sucesso'`, `atualizado_em` matching the exact timestamp of a same-pass 422 "Cannot add answer in a closed ticket" GLPI->Tiflux followup failure — meaning a `'reabertura_tiflux'/'erro'` write happened but was immediately overwritten in the SAME sync pass by `_encerrar_em_cascata()` re-firing (Tiflux still reports `is_closed=True` since the reopen failed) and rewriting the row back to `'encerramento'/'sucesso'`, burying the real error. This masking happens every pass, not just the next one — worse than the gap the original fix's docstring described. No `'reabertura_tiflux'` row has EVER existed in `api_glpi_tiflux_followups` in production (checked across all chamados) — #34018 was the very first live exercise of this code path. User asked Tiflux to grant the reopen permission to the API user; awaiting reply, tracked in HANDOFF.md BLOCKED. Masking bug not fixed yet — real blocker is upstream permission, fixing the masking alone wouldn't reopen anything | User reported GLPI #34018 reopened but Tiflux didn't follow; investigated live (read-only DB queries approved by user, then one user-approved production `reabrir_ticket()` call to Tiflux to surface the real error) | supersedes nothing; refines the "known accepted gap" described in the entry directly below (that entry assumed the masked-over state only showed up on the NEXT run — it actually happens same-run)

2026-09-16 | New `TifluxClient.reabrir_ticket()` (`PUT /tickets/{ticket_number}/reopen`) and `db_followups.obter_ultima_acao_cascata_sucesso()`; `_sincronizar_chamado_aberto()` now reopens the Tiflux ticket (new `_reabrir_tiflux_apos_recusa_glpi()`) when it finds the GLPI ticket open again, the Tiflux ticket still `is_closed`, and the last successful cascade-status row for this `id_glpi` was `tipo='encerramento'` — i.e. the GLPI side was reopened (solution refused, or reopened manually) after this integration's own cascade close, and nobody told Tiflux | User asked to investigate why refusing a solution in GLPI wasn't reopening the Tiflux ticket (GLPI #24984). Root cause, confirmed against that chamado's real audit rows: cascade status sync was one-directional (Tiflux status -> GLPI only, see 2026-09-09 "Cascading close"/"Cascading reopen" entries below) — `TifluxClient` had no method that ever wrote a status back to Tiflux. #24984 was cascade-closed 2026-09-16 11:48, the requester's refusal reopened it in GLPI and added followups #75966/#75970, both of which failed publishing to Tiflux with 422 "Cannot add answer in a closed ticket" (Tiflux #340181 still closed), and the cascade-close logic then re-fired and pushed GLPI back to Solucionado, fighting the refusal. Fix reuses the same "one row per chamado" cascade-status bucket (`direcao='tiflux_para_glpi'`, `id_origem=-id_glpi`) the existing encerramento/reabertura rows already use, writing `tipo='reabertura_tiflux'` on success so the check doesn't refire once converged. No special "refusal followup" content is built — once Tiflux is reopened, the refusal's GLPI followup(s) become normal pending followups and publish through the existing GLPI->Tiflux sync path on the same pass. Known accepted gap (documented in the new helper's docstring): if the Tiflux reopen call itself fails, the row is left `status='erro'`, `obter_ultima_acao_cascata_sucesso()` no longer reports a confirmed `'encerramento'`, and the next run falls through to the normal cascade-close check instead of retrying the reopen — a rare double-failure edge case, not solved here, would need per-attempt history instead of the single-row-per-chamado design | supersedes nothing; refines the one-directional assumption baked into the two 2026-09-09 cascade close/reopen entries below

2026-09-15 | Reinstalled Python 3.13 machine-wide (`C:\Program Files\Python313`, via `winget install --scope machine`, replacing the previous per-user-only install) and reinstalled `requirements.txt` into its machine-wide `site-packages` (`python -m pip install -r requirements.txt` with `$env:PYTHONNOUSERSITE=1`, run elevated) — not a code change, an operational fix to this machine | The `GLPI-Tiflux-Sync` Scheduled Task (see ARCHITECTURE.md/HANDOFF.md) was registered this session for the first time on this machine (it wasn't actually running before, despite HANDOFF.md describing it as already live) and immediately failed two ways in sequence: (1) `py`/`python.exe` not found at all — this machine's only Python was installed "for the current user only," invisible to the SYSTEM account the task runs as; (2) after the machine-wide reinstall fixed that, `ModuleNotFoundError: No module named 'psycopg2'` — `pip install` (even run elevated) kept reporting "already satisfied" from the per-user site-packages (`...\AppData\Roaming\Python\Python313\site-packages`), because Python always puts the CURRENT USER's site-packages on `sys.path` regardless of elevation (elevation raises privilege, it doesn't change which user profile `site.py` resolves), so pip never actually touched the machine-wide directory until `PYTHONNOUSERSITE=1` forced it off `sys.path` for that install. Confirmed fixed live: `logs/glpi_tiflux.log` shows clean runs post-fix (sondagem + followups, no traceback) | supersedes nothing; HANDOFF.md's "Windows Scheduled Task ... runs every 5 min" RISK note was describing an assumption, not a confirmed-running task, until this session

2026-09-15 | Added `timeout=config.timeout_http_segundos` (default 30s) to every `GlpiClient`/`TifluxClient` HTTP call; `GlpiClient.buscar_chamados_desde()`'s sequential ID-by-ID sondagem now fires in batches (`ThreadPoolExecutor`, size `config.tamanho_lote_sondagem`, default 10) instead of one probe at a time, with the batch size also capped by `min(max_furos_seguidos - furos_seguidos, limite_por_execucao - len(encontrados))` so it never fires more requests than the sequential version would've needed in the worst case; results are folded back in ID order after each batch (`executor.map` preserves input order regardless of completion order), so the returned list is identical to the sequential version for the same GLPI state | Second step on "execução muito demorada" (see 2026-09-15 Session entry below). No `timeout=` anywhere meant one hung request could block the whole cron run indefinitely — found while evaluating why runs were slow, not reported as a separate incident. Sondagem was identified as the safer of two parallelization candidates: `max_furos_seguidos=50` (default) means every steady-state run (GLPI already caught up) pays a fixed tail of up to 50 sequential probes with no DB writes involved, vs. the followups loop (`tamanho_pagina_followups=50` chamados/run, ~3-6 sequential calls each) which is likely the bigger cost but needs a Postgres connection-per-thread or write-locking scheme first (psycopg2 connections aren't safe for concurrent multi-thread writes) — deferred, flagged in HANDOFF.md NEXT. `HTTPAdapter(pool_maxsize=max(tamanho_lote_sondagem, 10))` mounted on the GLPI session so concurrent probes in a batch don't serialize behind the default 10-connection pool. Added a dedicated test (`test_padrao_misto_atravessando_varios_lotes_preserva_ordem`) with per-ID (not shared-FIFO) fake responses to catch any accidental cross-ID response mixup under concurrency — ran 15x locally with no flakiness | supersedes nothing

2026-09-15 | `GlpiClient` and `TifluxClient` now own a `requests.Session()` per instance (optional `session` constructor param, defaults to a fresh one) instead of calling module-level `requests.get/post/put` on every call | User reported the cron run taking too long. Every one of the ~15-20 GLPI/Tiflux calls per run (more during `buscar_chamados_desde()`'s sequential ID-by-ID sondagem) was opening a brand-new TCP+TLS connection instead of reusing a keep-alive one — pure overhead with no behavior change. `GlpiClient.autenticar()` now creates the session before `initSession` and threads it into the returned instance so even the auth call benefits; `encerrar_sessao()` closes it. `tests/fakes.py`'s `FakeRequests` grew a `Session()` method returning itself (already implements get/post/put and records to `chamadas`) so no test needed rewriting. Next candidate, not yet done: parallelize `buscar_chamados_desde()`'s sequential probing (`ThreadPoolExecutor`) — bigger win, more risk (GLPI rate limits, furos-seguidos ordering) | supersedes nothing

2026-09-14 | `processar_chamado()` now checks `TifluxClient.buscar_ticket_por_chamado_glpi()` (new — GET `/tickets?search=<id>&filter_by=all&client_ids=...`, result re-filtered client-side on the id appearing as a standalone number in `title`) right after fetching the GLPI ticket, before resolving mesa/creating anything; a match short-circuits to `_vincular_ticket_existente()` (title-prefix in GLPI, register `sucesso` with the found `numero_tiflux`, skip técnico/status/anexos) instead of creating a new ticket | User reported real duplicates: some chamados had to be opened manually in Tiflux on 2026-09-11 (Friday) during a token outage, and once the token was fixed on 2026-09-14 the automatic sync re-created them because there was no `api_glpi_tiflux` row yet. Confirmed live that manually-opened tickets follow the same `"<titulo> (<id_glpi>)"` title convention as `_montar_form_data`, and found 8 real duplicate pairs this way (GLPI #33870/#33885/#33896/#33898/#33913/#33921/#33922/#33923 — each with an orphaned manual ticket and a same-day duplicate the audit table pointed at instead). `filter_by=all` is required since some manual tickets may already be closed; `search` is fuzzy (matches title/client/desk/ticket number/responsible/description start per the spec) so results are re-filtered on a word-boundary regex against `title` only. Multiple matches return `erro` naming the candidates rather than auto-picking — a wrong link is permanent | supersedes nothing; also defuses most of the pre-existing `status='erro'`-with-`numero_tiflux` duplication path noted below, since retries go through `processar_chamado()` too

2026-09-10 | New `sync/forcar_sincronizacao.py` (public CLI entrypoint, `python -m sync.forcar_sincronizacao --id-glpi N`) plus `db_chamados.obter_estado_chamado()`, consumed by a new sibling PHP project `interface-web-api-glpi-tiflux` (query page + force-sync page) | User asked for a manual backup path after a chamado was skipped by the automatic sondagem — a way to force one specific ticket without waiting for/depending on cron windows. Routing logic keys off `numero_tiflux IS NOT NULL`, not `status`, specifically to avoid the known ticket-duplication bug (see ARCHITECTURE.md): a `status='erro'` row can still have a real Tiflux ticket from a failed technician-assignment retry, and calling `processar_chamado()` again would duplicate it. User explicitly chose "only force followups, never risk recreating" for the already-`sucesso` case over a riskier "force full recreation" option. Advisory lock (`pg_try_advisory_lock`) added on review to close a double-click/two-tab race; explicitly does not cover the separate cron task | supersedes nothing

2026-09-10 | `db_chamados.obter_proximo_id_para_sondar()` now resumes from the smallest id_glpi among the last `quantidade_registros_para_recuo` (default 10) CONFIRMED rows (`status IN ('sucesso','erro')`), replacing the fixed-50-raw-IDs `janela_releitura_sondagem` rewind from 2026-09-09 | User's own idea, given the fixed window's known gap (documented in HANDOFF.md RISKS): a chamado missed by more than 50 raw IDs' worth of intervening tickets still fell out of the rewind before ever being reconferred. Basing the recuo on confirmed rows instead of raw ID count makes it self-scale — a long stretch of `ignorado` (out-of-scope, never audited) chamados between confirmations now stretches the recuo further back automatically instead of a constant that can be too small. User chose `sucesso + erro` (both prove the ID existed at sondagem time) over `sucesso` only | supersedes the 2026-09-09 `janela_releitura_sondagem` (fixed 50-ID) entry

2026-09-09 | `db_chamados.obter_proximo_id_para_sondar()` now starts `janela_releitura_sondagem` (default 50) IDs before `MAX(id_glpi)+1`, not exactly at it | User reported chamado #33769 never synced despite existing in GLPI with the right observer group. Root cause: it existed but got probed and returned 404 at the exact moment (GLPI eventual-consistency lag or transient trash state), counted as a "furo," and was never revisited since sondagem always resumed from the max ID already seen (33772, already past it). Recovered #33769 manually. Fix rewinds the probe window each run so recently-missed IDs get reconferred; IDs already `status='sucesso'` are filtered back out by `obter_ids_ja_processados`, so this doesn't reprocess them | supersedes nothing

2026-09-09 | New `GlpiClient.voltar_status_para_novo()` (PUT /Ticket/{id}, status=1) called after `atribuir_tecnico` (ticket creation, `processamento_chamado.py`) and after every successful `criar_followup` (Tiflux->GLPI, `sincronizacao_followups.py`) | User reported chamados were ending up at GLPI status "Processando (atribuído)" instead of staying "Novo" — this GLPI instance auto-transitions Novo->Atribuído whenever a Ticket_User (actor) or a followup is added, which isn't a decision this integration should be making for the user. User explicitly confirmed (asked directly, given the cascade-reopen interaction) that this should apply ALWAYS, including after a cascade-reopen (status 2) gets a new followup — no special-casing for "already reopened once." Verified live on real chamado #33756 that GLPI actually accepts the backwards transition via PUT (even 5->1, not just 2->1) rather than silently refusing it like it does for other status changes (`_mensagem_de_recusa`) — side effect: this accidentally reopened #33756 from Solucionado back to Novo (test ticket, no impact) | supersedes nothing; not applied to cascade-close/reopen's own `atribuir_tecnico` call (line ~137 of `_encerrar_em_cascata`) since that's immediately followed by an explicit status change to Solucionado in the same call — no transient state to correct there

2026-09-09 | Ticket creation (`_montar_form_data`) now always sends the mandatory Tiflux custom field "Módulo Utilizado" (`entity_field_id=35107`) with option "Padrão" (`entity_field_option_id=819846`), via `entities[][entity_field_id]`/`entities[][value]` form keys (no index — `entities[0][...]` sends a Hash, not an Array, and the API rejects it) | User's request: this required field has no GLPI equivalent, so it's always set to "Padrão". Verified live against 4 existing tickets (`GET /tickets?include_filled_entity=true`) that the write format for a `single_select` field is the option ID as a string, matching the option's own `value` in the read response | supersedes nothing

2026-09-09 | `TifluxClient.criar_ticket()` now treats HTTP 207 as success (ticket created), not failure | Discovered live: as soon as the request includes `entities`, the Tiflux API returns 207 instead of 201 even on full success (documented: ticket is created regardless of `entities_errors`). The old code treated any non-200/201 as failure, so every ticket creation with the new "Módulo Utilizado" field was wrongly marked `erro` in the audit table — which would have retried and created a DUPLICATE ticket every cron cycle. Caught before re-enabling the scheduled task, but #33753 and #33756 had already been created for real (361535, 361536) during testing; recovered manually (title prefix + técnico assignment done by hand, audit rows corrected to `sucesso`) instead of letting them reprocess | supersedes nothing

2026-09-09 | `db_chamados.conectar_db()` now explicitly sets `client_encoding='UTF8'`; `registrar_resultado()` sanitizes `mensagem` (`_sanear_win1252`, replaces unencodable chars with `?`) before INSERT | Found live: without explicit client_encoding, libpq on Windows defaulted to cp1252 and `UnicodeEncodeError` on any non-latin1 character (e.g. '✪' in a client name) crashed the WHOLE script mid-batch — chamados after the offending one in that run's candidate list never got processed. Setting client_encoding=UTF8 alone wasn't enough: the audit database's actual server encoding is WIN1252 (fixed, can't change without recreating the DB), so Postgres itself then rejected the same characters with `UntranslatableCharacter`. Sanitizing before insert is the only fix that doesn't require migrating the DB | supersedes nothing; this bug likely explains past silent/partial cron runs, not just this session's testing

2026-09-09 | Private/internal communication is no longer synced in either direction: GLPI followups with `is_private=1` are dropped in `_followups_glpi_pendentes()` before reaching Tiflux, and Tiflux `/internal_communications` are no longer fetched/published in `sincronizar_followups_tiflux_para_glpi()` (`_publicar_comunicacoes_internas` removed) | User's explicit request: only public communication should sync from now on. `TifluxClient.publicar_comunicacao_interna`/`listar_comunicacoes_internas` kept (still valid API wrapper methods, just unused by the sync flow now) | supersedes the 2026-09-08 "bidirectional in one pass" and 2026-09-09 "prefixed... applies to public answers, internal communications, AND solution content" entries insofar as they covered internal communications — cascade-close solution content still uses public answers only, unaffected

2026-09-09 | Fixed a real echo/duplication bug: followups created by the integration (Tiflux -> GLPI, users_id always Léo or Sania) were getting synced back GLPI -> Tiflux as if they were new, duplicating the message in Tiflux | User reported it live, from an actual duplicate on Tiflux #361478. Root cause: `_followups_glpi_pendentes()` only excluded followups already forwarded (`obter_followups_glpi_ja_processados`, tracks the GLPI->Tiflux direction) — it never excluded followups the integration itself had just created via the OPPOSITE direction (Tiflux->GLPI), since those get a different audit-table `direcao` and were never checked. Fixed: `_followups_glpi_pendentes()` now also excludes any GLPI followup whose `users_id` is Léo (`config.id_glpi_leo`) or Sania (`config.id_glpi_sania`) — user's own rule: forward anything from GLPI EXCEPT authored by "me or Sania". Side effect (accepted, no way to distinguish): a followup a human genuinely writes as Léo or Sania directly in GLPI (not via this integration) is also filtered now | supersedes nothing; confirmed live on #33724/#361478 — pre-fix duplicates found in Tiflux's answer history (e.g. #31469080 duplicating #31468861's content, wrapped in the integration's own author-prefix) were reported to the user for manual cleanup, not deleted automatically

2026-09-09 | Scope of sync widened from GLPI group observer 22 (EMBRAS - Atendimentos) only, to groups 21 (EMBRAS - Backlog) OR 22 | User's explicit request. `Config.id_grupo_observador: int` became `ids_grupo_observador: tuple[int, ...] = (21, 22)`; `GlpiClient.chamado_tem_grupo_observador()` now checks membership in the tuple instead of equality against a single id | supersedes nothing; live-confirmed group 21 exists in GLPI and is named "EMBRAS - Backlog" as the user described

2026-09-09 | Followup/solution content synced Tiflux->GLPI is prefixed with `<strong>{nome}</strong> ({data} {hora})<br><br>` before the original text, where `nome`/`data`/`hora` reflect who ACTUALLY wrote it in Tiflux and when | User's request, prompted by ticket #361478: José Augusto posted an answer, but the GLPI followup only showed Sania as author (per the mesa rule) with no way to tell who really wrote it. Real GLPI authorship (`users_id`, technician assignment) deliberately still follows the mesa rule unchanged — this is a text-only marker, not a change to who owns the record in GLPI. Applies to public answers, internal communications, AND the solution content used at cascade-close (user explicitly asked for both) | supersedes nothing; live-verified on real chamado #33724/Tiflux #361478 (mesa SUPRIMENTOS) — José Augusto's real answer synced prefixed correctly, `users_id` stayed 4816 (Sania)

2026-09-09 | Date/time in the prefix is Tiflux's UTC timestamp (`answer_time` or `created_at`) minus a fixed 3 hours, formatted `dd/mm/aaaa hh:mm` | User's explicit choice: Brasília time, not raw UTC. Fixed offset instead of a timezone library, since Brazil has had no DST since 2019 — would need revisiting only if that policy changes | supersedes nothing

2026-09-09 | Discovered mid-session: the Windows Scheduled Task `GLPI-Tiflux-Sync` executes `glpi_tiflux.py` directly from this working directory every 5 minutes — not a separately deployed copy | Explains why chamado #33630's title already showed the new prefix before any deliberate test run, and why a live test of #361478 reported 0 new syncs (the scheduled task had already processed it seconds earlier, using the in-progress code). Told to the user; they chose to keep working directly rather than pause the task | supersedes nothing; not a code change, a process fact — logged so a future session doesn't assume uncommitted edits here are inert

2026-09-09 | `GlpiClient._atualizar_chamado()` (used by `encerrar_chamado`/`atualizar_titulo`) now treats a non-empty `message` in the `PUT /Ticket/{id}` response body as failure, even on HTTP 200/201 | Found live testing chamado #33733: this GLPI instance always returns HTTP 200 for `PUT /Ticket/{id}`, even when a business rule rejects the requested field change (e.g. `[{"33733":true,"message":"Técnico atribuído é obrigatório antes do chamado ser solucionado/fechado"}]`) — the previous code read HTTP 200 as unconditional success, so a rejected cascade-close would have silently no-op'd forever with no error ever logged or retried. New `_mensagem_de_recusa()` catches this | supersedes the original `_atualizar_chamado` behavior of trusting the HTTP status code alone; mocked tests never caught this because they were never told GLPI does this

2026-09-09 | Before attempting cascade-close (status -> Solucionado), `_encerrar_em_cascata()` first ensures the GLPI ticket has (a) a technician assigned (`Ticket_User` type=2) and (b) at least one `ITILSolution` — both via the same mesa-based `definir_autor_glpi()` rule already used for followup authorship, and both idempotent (checked via `tecnico_atribuido()`/`solucao_registrada()` before creating, so retries don't duplicate) | Found live testing #33733: this GLPI instance silently refuses status Solucionado/Fechado without both preconditions (confirmed by triggering each rejection in turn, then manually supplying each and watching status finally stick at 5). User's fix, given both requirements after being shown the live rejection messages: assign técnico by mesa (ARRECADAÇÃO -> Léo 4988, else -> Sania 4816) right at ticket creation (`processamento_chamado._atribuir_tecnico_glpi`, new), and for the solution content, use the ticket's most recent PUBLIC answer from Tiflux (`/answers`, picked by `answer_time` — user explicitly chose public answers only, not internal communications, and not "whichever type is newer") | supersedes nothing; end-to-end confirmed live on #33736/Tiflux #361499 — technician auto-assigned at creation, real posted answer synced then used verbatim as the GLPI solution content, status flipped 2->5

2026-09-09 | GLPI ticket title is prefixed with `#<numero_tiflux> - ` after the chamado is created in Tiflux, via new `GlpiClient.atualizar_titulo()` | User's explicit rule on branch v3, with a concrete example (GLPI #33684 titled `Solicito "Inativação de Acessos" - E-mails diversos` becomes `#361458 - Solicito "Inativação de Acessos" - E-mails diversos`). User confirmed the "(33684)" in their example was just the pre-existing GLPI ticket id already in the title, not something to append — only the `#<tiflux> - ` prefix is new | supersedes nothing

2026-09-09 | A failed title-update (`atualizar_titulo`) logs a warning and does NOT mark the chamado as 'erro' — chamado still counts as 'sucesso', same pattern as attachment failures, not the `atribuir_tecnico` pattern | User initially picked "conta como erro e reprocessa" (matching `atribuir_tecnico`), unaware that 'erro' reprocesses `_processar()` from the top — and since `_criar_ticket` already ran, that would create a DUPLICATE ticket in Tiflux (the same known bug noted in `db_followups.obter_chamados_para_varrer_followups`'s docstring for `atribuir_tecnico` failures). Flagged the consequence explicitly; user reversed the decision once they saw it | supersedes the "conta como erro e reprocessa" choice made earlier the same session, before the duplication consequence was identified

2026-09-09 | `GlpiClient.encerrar_chamado()` and new `atualizar_titulo()` share a private `_atualizar_chamado(id_chamado, campos, acao)` helper | Both are `PUT /Ticket/{id}` differing only in which `input` field they set (`status` vs `name`) and the action verb in the error message — avoids duplicating the request/error-handling boilerplate | supersedes nothing

2026-09-09 | Cascading close: chamado fechado/canceled in Tiflux (`is_closed=True`) closes the GLPI ticket too, at status 5 (Solucionado), only after pending followups are synced this same pass | User's explicit rule on branch v3: "eu encerro o chamado no tiflux e ele vai encerrar o chamado no GLPI." User chose Solucionado over Fechado (status 6) so the ticket stays touchable in GLPI; chose "sync followups first, then close" over closing immediately, so no reply gets stranded unsynced | supersedes nothing

2026-09-09 | Cascading reopen: if a chamado sitting at GLPI status 5 (Solucionado) has its Tiflux ticket reopened (`is_closed` flips back to False), the GLPI ticket is reopened too, at status 2 (Processando/atribuído) — but ONLY when GLPI status is exactly 5; any other closed status (e.g. 6 Fechado, set manually by a técnico) is left alone | User confirmed tickets do get reopened in Tiflux after being closed. Restricting to status 5 specifically avoids the integration undoing a human's deliberate manual closure in GLPI — it only undoes closures it caused itself | supersedes nothing; known gap in HANDOFF.md RISKS: a human manually setting status 5 for unrelated reasons would also get auto-reopened if Tiflux happens to be open

2026-09-09 | `TifluxClient.obter_mesa_do_ticket()` replaced by a general `obter_ticket()` (GET /tickets/{ticket_number}); `sincronizar_followups_tiflux_para_glpi()` now takes `ticket_tiflux` as a parameter instead of fetching it internally | Adding the cascade close/reopen check needed the same Tiflux ticket data (`is_closed`) that mesa resolution for followup authorship already needed (`desk.id`) — fetching once in `_sincronizar_chamado_aberto` and passing it down avoids two GETs per chamado per pass | supersedes the `obter_mesa_do_ticket()`-only version from the same-day authorship fix (never released past v3)

2026-09-08 | Followup sync ships bidirectional in one pass, not staged | User explicitly chose "both directions now" over a GLPI->Tiflux-only phase 1, after being warned about echo-prevention complexity | supersedes nothing

2026-09-08 | Followup POSTs (`/answers`, `/client-answers`, `/internal_communications`) use `files={"field": (None, value)}` instead of `data={...}` | Live-tested: `/internal_communications` returns 415 on `application/x-www-form-urlencoded`; it requires real `multipart/form-data`. `/answers` happened to tolerate the wrong encoding but was switched too for consistency with the documented contract | supersedes the first implementation attempt (used `data=` + `headers_tiflux_form`, worked for `/answers` only)

2026-09-08 | Followup content passed through as HTML unchanged in both directions, no plain-text conversion | Live-verified: GLPI `ITILFollowup.content` and Tiflux `answers.name`/`internal_communications.text` are both real HTML, unlike the ticket `description` field (which is plain text, hence `html_para_texto_plano()` exists only for that field) | supersedes an initial assumption that followups might need the same plain-text treatment as ticket descriptions

2026-09-08 | Applied `html.unescape()` defensively to all followup content before forwarding | Live data showed some GLPI followups return HTML-entity-encoded content (`&#60;p&#62;...`) instead of literal tags, depending on how they were created; `html.unescape()` is a no-op on already-literal HTML so it's safe both ways | none

2026-09-08 | New `api_glpi_tiflux_followups` table instead of extending `api_glpi_tiflux` | Existing table is one-row-per-ticket (`ON CONFLICT (id_glpi)`); followups are many-per-ticket. New table's conflict key is `(direcao, id_origem)` | none

2026-09-08 | Followup candidate queries (`obter_followups_glpi_ja_processados`, `obter_respostas_tiflux_ja_processadas_ou_proprias`) filter to `status='sucesso'` only | First implementation counted any row (including `status='erro'`) as "already handled," so a failed followup was silently never retried. Caught live during testing: a 415 error stayed permanently stuck until this fix | none

2026-09-08 | Closed-ticket skips write a bookkeeping row via `registrar_chamado_fechado_para_followups` (direcao='verificacao_status', id_origem=-id_glpi) | Without it, a closed ticket never gets a followups-table row and permanently sorts first in the "least-recently-checked" rotation (`obter_chamados_para_varrer_followups`), starving real candidates of the per-run budget | none

2026-09-08 | Tiflux->GLPI echo prevention relies primarily on the audit table, with `answer_origin=='api'`/`[API]`-author-prefix as a secondary filter for `/answers` only | `/internal_communications` responses have no equivalent field — confirmed by live testing that manually clearing followup audit rows causes a stale internal communication to be re-imported as a duplicate. Production code never deletes these rows, so this is a testing artifact, not a normal-operation risk | none

2026-09-08 | `glpi_tiflux.py` (~1100 lines) not split despite the 500-line/file guideline | Followup sync was added to the existing single-file script to match its established pattern (numbered `# === N. ===` sections, shared header dicts, shared `log()`/`conectar_db()`). Splitting now would be a larger, separately-reviewable refactor; flagged as a risk in state/HANDOFF.md rather than done silently mid-feature | none

2026-09-08 | Split `glpi_tiflux.py` into `sync/` package (config, db_chamados, db_followups, glpi_client, tiflux_client, html_texto, regras_negocio, processamento_chamado, sincronizacao_followups, main); `glpi_tiflux.py` kept as a 10-line entrypoint shim so the cron invocation path doesn't change | Per CLAUDE.md's 500-line/file and 4-20-line/function guidelines, flagged as a risk since the previous entry. `CRED = carregar_credenciais()` at module scope was a blocker for testing (any import would need `credenciais.txt` on disk) — replaced with a frozen `Config` dataclass built once in `main()` and threaded through as a parameter. `headers_glpi`/module-global `_mesas_validas_do_cliente` replaced with `GlpiClient`/`TifluxClient` instances built once per run, to avoid cross-module global state. No SQL text changed (verified via AST diff against the pre-split file); all `requests.*` call sites and error-message wording preserved verbatim | supersedes the previous "not split" entry

2026-09-08 | `definir_tecnico()` only auto-assigns a technician for mesa ARRECADAÇÃO (Léo Alves); every other mesa is created in Tiflux with no responsible technician set | User instruction: stop assigning Sânia Almeida to non-ARRECADAÇÃO tickets — those should be left unassigned instead. `Config.id_tecnico_sania` removed (dead once `definir_tecnico` stopped returning it). `processar_chamado` now skips the `atribuir_tecnico` API call entirely when `definir_tecnico` returns `None`, rather than calling it with a null id | supersedes the previous "any other mesa -> Sânia" default in `definir_tecnico`

2026-09-09 | Tiflux->GLPI followups now send `users_id` on `ITILFollowup`, picked by mesa via new `definir_autor_glpi()` (mesa ARRECADAÇÃO -> Léo id 4988 in GLPI, any other mesa -> Sania id 4816 in GLPI), independent of which technician is assigned to the ticket in Tiflux | User report: followups from Sania's tickets in Tiflux were landing in GLPI under Léo's name, because `criar_followup()` never sent `users_id` and GLPI defaulted authorship to the API's authenticated session user (always Léo). This is a separate rule from `definir_tecnico()` (mesa ARRECADAÇÃO-only, sets the Tiflux responsible on GLPI->Tiflux ticket creation) — same mesa key, different direction and different meaning; don't merge them | supersedes nothing; NOT live-verified that GLPI actually persists a client-supplied `users_id` on `ITILFollowup` rather than overwriting it server-side — flagged in HANDOFF.md

2026-09-09 | Mesa used by `definir_autor_glpi()` comes from a live `GET /tickets/{ticket_number}` call to Tiflux (new `TifluxClient.obter_mesa_do_ticket()`, reads `desk.id`), not from the GLPI category recorded when the ticket was first created | User confirmed tickets do get moved between mesas in Tiflux after creation; deriving mesa from the original GLPI category (`depara_categoria(ticket["itilcategories_id"])`) would go stale after a re-route and misattribute followups. Costs one extra GET per `sincronizar_followups_tiflux_para_glpi()` call | supersedes the same-day "derive mesa from ticket['itilcategories_id']" attempt (never committed)

2026-09-08 | `log()` (sync/config.py) catches `UnicodeEncodeError` and re-encodes with `errors="replace"` instead of letting the print crash the whole run | Found live: running `glpi_tiflux.py` manually on this Windows machine (cp1252 console) crashed with `UnicodeEncodeError` on the emoji in `buscar_chamados_desde()`'s unconditional final log line — a line that runs on *every* invocation. This would have silently failed the newly-created 5-minute Task Scheduler job on any run that reaches that log call, i.e. effectively always once caught up with GLPI. Accented characters still get garbled (mojibake) under cp1252 output, but the process no longer crashes; the audit trail in Postgres is unaffected either way | none
